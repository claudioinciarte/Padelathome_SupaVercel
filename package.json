{
  "name": "p_at_home_backend",
  "version": "3.0.0",
  "description": "Sistema de reservas de pistas de pádel para residencias. Backend Express desplegable en Vercel (serverless) con base de datos Supabase (PostgreSQL).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --runInBand --detectOpenHandles",
    "test:coverage": "jest --coverage",
    "migrate": "node-pg-migrate"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "date-fns": "^3.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ics": "^3.7.6",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^4.2.1",
    "node-pg-migrate": "^7.5.2",
    "nodemailer": "^6.9.14",
    "pg": "^8.20.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
